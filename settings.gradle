// Lets Gradle auto-download the JDK 21 toolchain (build.gradle) when it isn't installed locally.
plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

rootProject.name = 'ChemCraft'
